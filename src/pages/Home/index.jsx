import { Search, Hero, Gallery, Destinations } from "../../components"

export default function index() {
    return (
        <section>
            <Search />
            <Hero />
            <Gallery />
            <Destinations />
        </section>
    )
}